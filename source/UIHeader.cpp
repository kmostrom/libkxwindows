
////////////////////////////////////////////////////////////////////////////////

#include <kxWindows/UIHeader.h>

////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::ClearFilter(int filter)
{
	return ::SendMessage(mWindowHandle, HDM_CLEARFILTER, (WPARAM) mWindowHandle, (LPARAM) filter);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::CreateDragImage(int index)
{
	return ::SendMessage(mWindowHandle, HDM_CREATEDRAGIMAGE, (WPARAM) index, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::DeleteItem(int index)
{
	return ::SendMessage(mWindowHandle, HDM_DELETEITEM, (WPARAM) index, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::EditFilter(int column, BOOL discardChanges)
{
	return ::SendMessage(mWindowHandle, HDM_EDITFILTER, (WPARAM) column, MAKELPARAM(discardChanges, 0));
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::GetBitmapMargin(void)
{
	return ::SendMessage(mWindowHandle, HDM_GETBITMAPMARGIN, (WPARAM) 0, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::GetImagelist(void)
{
	return ::SendMessage(mWindowHandle, HDM_GETIMAGELIST, (WPARAM) 0, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::GetItem(int index, LPHDITEM headerItem)
{
	return ::SendMessage(mWindowHandle, HDM_GETITEM, (WPARAM) index, (LPARAM) headerItem);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::GetItemCount(void)
{
	return ::SendMessage(mWindowHandle, HDM_GETITEMCOUNT, (WPARAM) 0, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::GetItemRect(int index, LPRECT rect)
{
	return ::SendMessage(mWindowHandle, HDM_GETITEMRECT, (WPARAM) index, (LPARAM) rect);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::GetOrderArray(int size, LPINT array)
{
	return ::SendMessage(mWindowHandle, HDM_GETORDERARRAY, (WPARAM) size, (LPARAM) array);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::GetUnicodeFormat(void)
{
	return ::SendMessage(mWindowHandle, HDM_GETUNICODEFORMAT, (WPARAM) 0, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::HitTest(LPHDHITTESTINFO hitTestInfo)
{
	return ::SendMessage(mWindowHandle, HDM_HITTEST, (WPARAM) 0, (LPARAM) hitTestInfo);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::InsertItem(int index, LPHDITEM item)
{
	return ::SendMessage(mWindowHandle, HDM_INSERTITEM, (WPARAM) index, (LPARAM) item);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::Layout(LPHDLAYOUT headerLayout)
{
	return ::SendMessage(mWindowHandle, HDM_LAYOUT, (WPARAM) 0, (LPARAM) headerLayout);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::OrderToIndex(int order)
{
	return ::SendMessage(mWindowHandle, HDM_ORDERTOINDEX, (WPARAM) order, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::SetBitmapMargin(int width)
{
	return ::SendMessage(mWindowHandle, HDM_SETBITMAPMARGIN, (WPARAM) width, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::SetFilterChangeTimeout(int timeout)
{
	return ::SendMessage(mWindowHandle, HDM_SETFILTERCHANGETIMEOUT, (WPARAM) 0, (LPARAM) timeout);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::SetHotDivider(BOOL coord, DWORD inputValue)
{
	return ::SendMessage(mWindowHandle, HDM_SETHOTDIVIDER, (WPARAM) coord, (LPARAM) inputValue);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::SetImagelist(HIMAGELIST imageListHandle)
{
	return ::SendMessage(mWindowHandle, HDM_SETIMAGELIST, (WPARAM) 0, (LPARAM) imageListHandle);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::SetItem(int index, LPHDITEM item)
{
	return ::SendMessage(mWindowHandle, HDM_SETITEM, (WPARAM) index, (LPARAM) item);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::SetOrderArray(int size, LPINT array)
{
	return ::SendMessage(mWindowHandle, HDM_SETORDERARRAY, (WPARAM) size, (LPARAM) array);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIHeader::SetUnicodeFormat(BOOL unicode)
{
	return ::SendMessage(mWindowHandle, HDM_SETUNICODEFORMAT, (WPARAM) unicode, (LPARAM) 0);
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////
