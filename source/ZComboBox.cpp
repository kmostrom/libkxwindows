////////////////////////////////////////////////////////////////////////////////

#include "ZComboBox.h"

////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::AddString(TCHAR *szString)
{
	return(::SendMessage(_hWnd, CB_ADDSTRING, (WPARAM)0, (LPARAM)szString));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::DeleteString(long lIndex)
{
	return(::SendMessage(_hWnd, CB_DELETESTRING, (WPARAM)lIndex, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::Dir(UINT uiAttrib, LPCSTR szFilename)
{
	return(::SendMessage(_hWnd, CB_DIR, (WPARAM)uiAttrib, (LPARAM)szFilename));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::FindString(long lIndex, LPCSTR szQuery)
{
	return(::SendMessage(_hWnd, CB_FINDSTRING, (WPARAM)lIndex, (LPARAM)szQuery));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::FindStringExact(long lIndex, LPCSTR szQuery)
{
	return(::SendMessage(_hWnd, CB_FINDSTRINGEXACT, (WPARAM)lIndex, (LPARAM)szQuery));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetComboboxInfo(COMBOBOXINFO *pComboBoxInfo)
{
	return(::SendMessage(_hWnd, CB_GETCOMBOBOXINFO, (WPARAM)0, (LPARAM)pComboBoxInfo));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetCount(void)
{
	return(::SendMessage(_hWnd, CB_GETCOUNT, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetCurSel(void)
{
	return(::SendMessage(_hWnd, CB_GETCURSEL, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetDroppedControlRect(LPRECT pDroppedControlRect)
{
	return(::SendMessage(_hWnd, CB_GETDROPPEDCONTROLRECT,(WPARAM)0, (LPARAM)pDroppedControlRect));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetDroppedState(void)
{
	return(::SendMessage(_hWnd, CB_GETDROPPEDSTATE, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetDroppedWidth(void)
{
	return(::SendMessage(_hWnd, CB_GETDROPPEDWIDTH, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetEditSel(DWORD dwStartPos, DWORD dwEndPos)
{
	return(::SendMessage(_hWnd, CB_GETEDITSEL, (WPARAM)dwStartPos, (LPARAM)dwEndPos));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetExtendedUI(void)
{
	return(::SendMessage(_hWnd, CB_GETEXTENDEDUI, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetHorizontalExtent(void)
{
	return(::SendMessage(_hWnd, CB_GETHORIZONTALEXTENT, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetItemData(long lIndex)
{
	return(::SendMessage(_hWnd, CB_GETITEMDATA, (WPARAM)lIndex, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetItemHeight(long lIndex)
{
	return(::SendMessage(_hWnd, CB_GETITEMHEIGHT, (WPARAM)lIndex, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetLbText(long lIndex, LPCSTR szString)
{
	return(::SendMessage(_hWnd, CB_GETLBTEXT, (WPARAM)lIndex, (LPARAM)szString));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetLbTextLen(long lIndex)
{
	return(::SendMessage(_hWnd, CB_GETLBTEXTLEN, (WPARAM)lIndex, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetLocale(void)
{
	return(::SendMessage(_hWnd, CB_GETLOCALE, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetMinVisible(void)
{
	return(::SendMessage(_hWnd, CB_GETMINVISIBLE, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::GetTopIndex(void)
{
	return(::SendMessage(_hWnd, CB_GETTOPINDEX, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::InitStorage(long lItemCount, long lBytes)
{
	return(::SendMessage(_hWnd, CB_INITSTORAGE, (WPARAM)lItemCount, (LPARAM)lBytes));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::InsertString(long lIndex, TCHAR *szString)
{
	return(::SendMessage(_hWnd, CB_INSERTSTRING, (WPARAM)lIndex, (LPARAM)szString));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::LimitText(long lMaxChars)
{
	return(::SendMessage(_hWnd, CB_LIMITTEXT, (WPARAM)lMaxChars, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::ResetContent(void)
{
	return(::SendMessage(_hWnd, CB_RESETCONTENT, (WPARAM)0, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SelectString(long lIndex, LPCSTR szString)
{
	return(::SendMessage(_hWnd, CB_SELECTSTRING, (WPARAM)lIndex, (LPARAM)szString));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetCurSel(long lIndex)
{
	return(::SendMessage(_hWnd, CB_SETCURSEL, (WPARAM)lIndex, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetDroppedWidth(long lWidth)
{
	return(::SendMessage(_hWnd, CB_SETDROPPEDWIDTH, (WPARAM)lWidth, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetEditSel(long lStartPos, long lEndPos)
{
	return(::SendMessage(_hWnd, CB_SETEDITSEL, (WPARAM)0, (LPARAM)MAKELPARAM(lStartPos, lEndPos)));	
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetExtendedUI(BOOL bFlag)
{
	return(::SendMessage(_hWnd, CB_SETEXTENDEDUI, (WPARAM)bFlag, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetHorizontalExtent(long lWidth)
{
	return(::SendMessage(_hWnd, CB_SETHORIZONTALEXTENT, (WPARAM)lWidth, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetItemData(long lIndex, DWORD dwData)
{
	return(::SendMessage(_hWnd, CB_SETITEMDATA, (WPARAM)lIndex, (LPARAM)dwData));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetItemHeight(long lIndex, long lHeight)
{
	return(::SendMessage(_hWnd, CB_SETITEMHEIGHT, (WPARAM)lIndex, (LPARAM)lHeight));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetLocale(WORD wLocale)
{
	return(::SendMessage(_hWnd, CB_SETLOCALE, (WPARAM)wLocale, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetMinVisible(long lMinVisible)
{
	return(::SendMessage(_hWnd, CB_SETMINVISIBLE, (WPARAM)lMinVisible, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::SetTopIndex(long lIndex)
{
	return(::SendMessage(_hWnd, CB_SETTOPINDEX, (WPARAM)lIndex, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT ZComboBox::ShowDropdown(BOOL bFlag)
{
	return(::SendMessage(_hWnd, CB_SHOWDROPDOWN, (WPARAM)bFlag, (LPARAM)0));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////
