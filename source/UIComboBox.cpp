////////////////////////////////////////////////////////////////////////////////

#include <kxWindows/UIComboBox.h>

#include <windows.h>

////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::AddString(TCHAR *string)
{
	return ::SendMessage(mWindowHandle, CB_ADDSTRING, (WPARAM) 0, (LPARAM) string);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::DeleteString(long index)
{
	return ::SendMessage(mWindowHandle, CB_DELETESTRING, (WPARAM) index, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::Dir(UINT attributes, LPCSTR filename)
{
	return ::SendMessage(mWindowHandle, CB_DIR, (WPARAM) attributes, (LPARAM) filename);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::FindString(long index, LPCSTR query)
{
	return ::SendMessage(mWindowHandle, CB_FINDSTRING, (WPARAM) index, (LPARAM) query);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::FindStringExact(long index, LPCSTR query)
{
	return ::SendMessage(mWindowHandle, CB_FINDSTRINGEXACT, (WPARAM) index, (LPARAM) query);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetComboboxInfo(COMBOBOXINFO *comboBoxInfo)
{
	return ::SendMessage(mWindowHandle, CB_GETCOMBOBOXINFO, (WPARAM) 0, (LPARAM) comboBoxInfo);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetCount(void)
{
	return ::SendMessage(mWindowHandle, CB_GETCOUNT, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetCurSel(void)
{
	return ::SendMessage(mWindowHandle, CB_GETCURSEL, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetDroppedControlRect(LPRECT droppedControlRect)
{
	return ::SendMessage(mWindowHandle, CB_GETDROPPEDCONTROLRECT,(WPARAM) 0, (LPARAM) droppedControlRect);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetDroppedState(void)
{
	return ::SendMessage(mWindowHandle, CB_GETDROPPEDSTATE, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetDroppedWidth(void)
{
	return ::SendMessage(mWindowHandle, CB_GETDROPPEDWIDTH, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetEditSel(DWORD startPosition, DWORD endPosition)
{
	return ::SendMessage(mWindowHandle, CB_GETEDITSEL, (WPARAM) startPosition, (LPARAM) endPosition);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetExtendedUI(void)
{
	return ::SendMessage(mWindowHandle, CB_GETEXTENDEDUI, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetHorizontalExtent(void)
{
	return ::SendMessage(mWindowHandle, CB_GETHORIZONTALEXTENT, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetItemData(long index)
{
	return ::SendMessage(mWindowHandle, CB_GETITEMDATA, (WPARAM) index, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetItemHeight(long index)
{
	return ::SendMessage(mWindowHandle, CB_GETITEMHEIGHT, (WPARAM) index, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetLbText(long index, LPCSTR string)
{
	return ::SendMessage(mWindowHandle, CB_GETLBTEXT, (WPARAM) index, (LPARAM) string);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetLbTextLen(long index)
{
	return ::SendMessage(mWindowHandle, CB_GETLBTEXTLEN, (WPARAM) index, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetLocale(void)
{
	return ::SendMessage(mWindowHandle, CB_GETLOCALE, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetMinVisible(void)
{
	return ::SendMessage(mWindowHandle, CB_GETMINVISIBLE, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::GetTopIndex(void)
{
	return ::SendMessage(mWindowHandle, CB_GETTOPINDEX, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::InitStorage(long itemCount, long bytes)
{
	return ::SendMessage(mWindowHandle, CB_INITSTORAGE, (WPARAM) itemCount, (LPARAM) bytes);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::InsertString(long index, TCHAR *string)
{
	return ::SendMessage(mWindowHandle, CB_INSERTSTRING, (WPARAM) index, (LPARAM) string);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::LimitText(long maxCharacters)
{
	return ::SendMessage(mWindowHandle, CB_LIMITTEXT, (WPARAM) maxCharacters, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::ResetContent(void)
{
	return ::SendMessage(mWindowHandle, CB_RESETCONTENT, (WPARAM) 0, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SelectString(long index, LPCSTR string)
{
	return ::SendMessage(mWindowHandle, CB_SELECTSTRING, (WPARAM) index, (LPARAM) string);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetCurSel(long index)
{
	return ::SendMessage(mWindowHandle, CB_SETCURSEL, (WPARAM) index, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetDroppedWidth(long width)
{
	return ::SendMessage(mWindowHandle, CB_SETDROPPEDWIDTH, (WPARAM) width, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetEditSel(long startPosition, long endPosition)
{
	return ::SendMessage(mWindowHandle, CB_SETEDITSEL, (WPARAM) 0, (LPARAM) MAKELPARAM(startPosition, endPosition));	
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetExtendedUI(BOOL flag)
{
	return ::SendMessage(mWindowHandle, CB_SETEXTENDEDUI, (WPARAM) flag, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetHorizontalExtent(long width)
{
	return ::SendMessage(mWindowHandle, CB_SETHORIZONTALEXTENT, (WPARAM) width, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetItemData(long index, DWORD data)
{
	return ::SendMessage(mWindowHandle, CB_SETITEMDATA, (WPARAM) index, (LPARAM) data);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetItemHeight(long index, long height)
{
	return ::SendMessage(mWindowHandle, CB_SETITEMHEIGHT, (WPARAM) index, (LPARAM) height);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetLocale(WORD locale)
{
	return ::SendMessage(mWindowHandle, CB_SETLOCALE, (WPARAM) locale, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetMinVisible(long minVisible)
{
	return ::SendMessage(mWindowHandle, CB_SETMINVISIBLE, (WPARAM) minVisible, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::SetTopIndex(long index)
{
	return ::SendMessage(mWindowHandle, CB_SETTOPINDEX, (WPARAM) index, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////

LRESULT UIComboBox::ShowDropdown(BOOL flag)
{
	return ::SendMessage(mWindowHandle, CB_SHOWDROPDOWN, (WPARAM) flag, (LPARAM) 0);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////
