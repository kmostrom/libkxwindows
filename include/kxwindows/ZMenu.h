/////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#ifndef _ZMENU_H_
#define _ZMENU_H_

#include <windows.h>

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class ZMenu
{
public:
	ZMenu(void);
	~ZMenu(void);

	HMENU LoadMenu(UINT uiMenuID);
	bool Enable(UINT uiMenuItemID, UINT uiEnable = MF_ENABLED);
	HMENU	GetMenuHandle(void);
	void	SetMenu(HWND hWndParent);
	HMENU	GetSubMenu(int iPos);

private:
	HMENU _hMenu;
	UINT _uiMenuID;
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#endif